name: omdbmovies
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  get: ^4.1.4
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  faker: ^1.3.0
  mockito: ^5.0.7
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
